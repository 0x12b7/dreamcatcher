{
	"name": "reliq",
	"description": "Reliq is a library for building unreasonably safe TypeScript applications.",
	"version": "2.0.0",
	"license": "Apache-2.0",
	"type": "module",
	"types": "target/tslib/mod.d.cts",
	"main": "target/tslib/mod.cjs",
	"scripts": {
		"build": "bun run script/build_script.ts",
		"link": "bun run script/link.ts",
		"blink": "bun run link && bun run build",
		"release": "bun run build && npm publish"
	},
	"devDependencies": {
		"@types/bun": "1.1.14",
		"tsup": "8.3.5"
	},
	"peerDependencies": {
		"typescript": "5.7.2"
	}
}