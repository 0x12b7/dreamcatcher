// SPDX-License-Identifier: Apache-2.0
pragma solidity >=0.8.19;
import {Math} from "./Math.sol";

library StakingRewardsDistributionMath {
    function previewRewards(uint256 supplyLeft, uint256 supplyStaked, uint256 duration)
        internal
        pure
        returns (uint256) {
        
    }
}